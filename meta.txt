#Development -- Major / Important
V opening banner like Worldnames
V suitability for use on mobile devices
- suitability of slider bar // colour coding
V top forenames historic // male, female
V top forenames recent // male, female
O top areas // parishes (modal occurrence)
O top areas // postcodes (modal occurrence)
V top consumer categories // modal OAC category
V top consumer categories // modal IMD score
- top consumer categories // mean IMD score
V top consumer categories // modal health access
V top consumer categories // modal internet user classification
V top consumer categories // modal broad band speed
V top consumer categories // modal consumer vulnerability
O location tab // same statistics, postcode based (type in postcode // determine location, get statistics output area) 

#Development -- Minor 
V reduce clutter and text
- FAQ (detail on methodology, how 'top' is defined, data sources, etc.)
V link to World Statistics ('see Worldnames mapping website')
V link to personal website

#Development -- To discuss
- surname roots ('feed from Worldnames')

#Development -- Bugs
V frequencies pre-rendered names incorrect if no occurrence in historic data (e.g. musa) // update all pre-stored frequencies through kde.kde_lookup
- on the fly calculation (broken after reorganisation)
V forename frequencies
- top areas ('Isle of Man' large output area issue)

#Development -- Server


#Deployment
1 Set up Nginx
2a Check vulnerabilities: python manage.py check --deploy 
2b Check vulnerabilities: https://www.ponycheckup.com/

#Temp deployment:Start
source activate webapp
cd /home/justin/webapp-project
gunicorn --bind 0.0.0.0:8080 webapp.wsgi

#Temp deployment:Stop
ps aux // ps aux | grep 'gunicorn'
kill <PID>